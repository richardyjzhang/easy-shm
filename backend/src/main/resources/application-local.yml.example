# 复制此文件为 application-local.yml，填入真实的数据库连接信息
# application-local.yml 已被 .gitignore 忽略，不会提交到仓库

spring:
  datasource:
    druid:
      mysql:
        url: jdbc:mysql://localhost:3306/easy_shm?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
        username: root
        password: 123456

      tdengine:
        url: jdbc:TAOS-WS://localhost:6041
        username: root
        password: taosdata
